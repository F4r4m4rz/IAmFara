import React from "react";

type SkillProps = {
    title: string;
    description?: string;
    rate: number;
}

const Skill = (props: SkillProps) => {

    return (
        <div>
            
        </div>
    )
}

export default Skill;